FROM nginx:1.27-alpine
COPY --chown=nginx:nginx out /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE {{APP_PORT}}
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 CMD wget --no-verbose --tries=1 --spider http://127.0.0.1:{{APP_PORT}}/ || exit 1
