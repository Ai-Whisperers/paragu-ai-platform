services:
  web:
    image: 3md-website:prod
    deploy:
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.docker.network=agent-net"
        - "traefik.http.routers.3md-website.entrypoints=websecure"
        - "traefik.http.routers.3md-website.rule=Host(`3md-website.paragu-ai.com`)"
        - "traefik.http.routers.3md-website.tls=true"
        - "traefik.http.routers.3md-website.tls.certresolver=letsencryptresolver"
        - "traefik.http.services.3md-website.loadbalancer.server.port=3000"
    networks:
      - agent-net
networks:
  agent-net:
    external: true
