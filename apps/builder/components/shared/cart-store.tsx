/**
 * Compat barrel — historical import path used by `universal/tienda-client`
 * and `universal/ui/product-card`. Canonical impl lives at
 * `@/components/universal/shared/cart-store`.
 *
 * Do not fork logic here — thin re-export only.
 */
export * from '@/components/universal/shared/cart-store'
