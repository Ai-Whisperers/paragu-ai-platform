/**
 * Compat barrel — historical import path used by universal helpers and
 * some app routes. Canonical implementation lives at
 * `@/lib/universal/config/config`. Keep this file thin; add re-exports
 * only, do not fork logic here.
 */
export * from '@/lib/universal/config/config'
