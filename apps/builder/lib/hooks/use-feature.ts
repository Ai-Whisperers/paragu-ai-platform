/**
 * Compat barrel — historical import path used by universal feature-provider.
 * Canonical impl lives at `@/lib/universal/hooks/use-feature`.
 */
export { FeatureContext, useFeature } from '@/lib/universal/hooks/use-feature'
