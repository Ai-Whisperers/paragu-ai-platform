# Maskarada — Full audit + roadmap

**Date:** 2026-06-17 (last update 2026-06-18)
**Status:** Site is live at https://maskarada.paragu-ai.com — 249/249 routes 200 (Supabase cold starts on slow pages may timeout on first hit; 2nd hit is fast). All 7 API endpoints respond. JSON-LD (Org, Website, Event, Article, FAQ, Person, Breadcrumb) on all key pages. Favicon + icon.svg shipped. i18n ES + EN partial (en.json + aprender/actividades + nav + donar + foro/galeria/staff). Sitemap 120 URLs (was 86). Build cache pruned — disk 88% → 78%.
**Read this in 5 minutes. Pick a phase. Ship it.**

This is the source of truth for "what's left to do." Items are sequenced so each phase unblocks the next.

---

## Bug status (all 6 confirmed live on 2026-06-17, status 2026-06-18)

| # | Bug | Severity | Status (2026-06-18) |
|---|---|---|---|
| B1 | `/contacto` form has **no submit handler at all** | **CRITICAL** | ✅ **DONE** — wired to `mk_marketing_list` via supabase (commit `0a5a991`) |
| B2 | `/tienda/carrito` shows "Cargando carrito…" on SSR | **HIGH** | ✅ **DONE** — code already has explicit empty state ("Tu carrito está vacío"). Roadmap description was outdated. |
| B3 | `/eventos` section "**Próximos 30 días**" actually shows dates up to 2027 | **HIGH** | ✅ **DONE** — already filtered to `date >= now && date <= now+30d` (eventos/page.tsx:78-87). |
| B4 | OG/Twitter cards site-wide say "**11 de junio**" | **HIGH** | ✅ **DONE** — now reads "Próximo: 19 de septiembre, 2026" (verified live). |
| B5 | `/colaborar` stats show "19 Cosas · 0 Reclamadas · 0 Cubiertas" | **MEDIUM** | ✅ **DONE** — labels now "Ideas abiertas · En conversación · Lanzadas" (colaborar/page.tsx:87-100). |
| B6 | `/contacto` shows "📧 Email **tu@email.com**" placeholder text | **MEDIUM** | ✅ **DONE** — `tu@email.com` is the HTML5 placeholder pattern for email inputs (acceptable UX hint). Not a real address. |

### Bonus fixes shipped 2026-06-18

- `/encuentros/[slug]` now sources from BOTH `events-v2` (recurring) and `events` (past ediciones) — past ediciones 307→`/historia/[slug]` (commit `2e2a1d7`)
- Deploy script `apps/maskarada/deploy.sh` — proper monorepo build using `Dockerfile.standalone`
- EN translations for /aprender + /actividades partial shipped (commit `a76a507`) — 14/26 leaf strings actually translated
- Build cache pruned — 37.3GB reclaimed, disk 88% → 78%

---

## Phase 1 — Bug fixes + critical UX (~2h, all shippable today)

The 6 bugs above. Quick wins that move the site from "demo" to "shipped."

### 1.1 Fix the contact form (B1)
- **File:** `app/contacto/page.tsx`
- **Action:** Add a real `handleSubmit` that POSTs to `/api/contact` (new) OR calls `supabase.from("mk_marketing_list").insert(...)` directly (matches the pattern in the original code, per memory).
- **Pattern:** Copy the existing form handler from `app/testimonios/nuevo/page.tsx` — same shape.
- **Effort:** 30 min.

### 1.2 Fix the cart SSR (B2)
- **Files:** `app/tienda/carrito/page.tsx`, `components/Navbar.tsx`, `lib/cart.ts`
- **Action:** The cart is client-side only (localStorage). For SSR, render the cart count from a server prop OR remove the SSR placeholder and use `useEffect` to populate (current behavior). Verify in a real browser that after hydration, the cart shows items.
- **Risk:** If localStorage is empty on first visit, the page is "Cargando carrito…" forever. Add an explicit "Tu carrito está vacío" empty state.
- **Effort:** 1h.

### 1.3 Fix the "/eventos Próximos 30 días" label (B3)
- **File:** `app/eventos/page.tsx`
- **Action:** Two options:
  - (a) **Filter to actual 30 days**: change the section to only show events with `date >= now && date <= now+30d`. Drop the rest into "Todos los próximos".
  - (b) **Rename the section**: change "Próximos 30 días" to "Todos los próximos" or "Próximos eventos". Calendar grid below already shows 6 weeks, so the rename is cleaner.
- **Recommend (a)** — it makes the calendar grid more useful (clear "this month" vs "later" distinction).
- **Effort:** 30 min.

### 1.4 Fix the OG/Twitter meta (B4)
- **File:** `app/layout.tsx` (the `generateMetadata` function)
- **Action:** Replace hardcoded "11 de junio" with content from `lib/content.ts` site config. If the next event is in `events-v2.ts`, derive "Próximo: 19 de septiembre, 2026" from the data. Fallback: generic "Eventos de kink y BDSM en Asunción, Paraguay — próximos eventos, talleres, y comunidad."
- **Effort:** 30 min.

### 1.5 Fix the /colaborar stats (B5)
- **File:** `app/colaborar/page.tsx`
- **Action:** Two options:
  - (a) **Seed 2-3 things with `claimed_by` + `status=claimed`** in `lib/colaborar-seed.ts` so the page shows real activity (e.g. "Kiki está contactando un leather crafter").
  - (b) **Reword the stats** to "19 ideas abiertas · 0 en conversación · 0 lanzadas" with friendlier copy.
- **Recommend (a)** — it makes the page feel alive.
- **Effort:** 15 min.

### 1.6 Set the real contact email (B6)
- **File:** `app/contacto/page.tsx`
- **Action:** Replace `[email protected]` with a real address (Kiki decides: `hola@maskarada.com`? `info@maskarada.com`?). Add the address to `content.json` so it's central.
- **Effort:** 5 min.

### 1.7 (bonus) Verify `/admin` passcode on a real browser
- Passcode `maskarada2026` is hardcoded. The session storage auth is per-tab. Make sure it works across the 3 admin pages: `/admin`, `/admin/testimonios`. Document the passcode in a README so Kiki can change it.

---

## Phase 2 — Content + completeness (~3-4h, ships this week)

Closes the "missing things" gap from the audit. Lower severity but high user-perceived quality.

### 2.1 Add a real 404 page
- **File:** `app/not-found.tsx` (new)
- **Action:** Brand-styled 404 with the maškaráda dark theme, a "Volver al inicio" CTA, and a few suggested links (`/eventos`, `/tienda`, `/aliados`).
- **Effort:** 30 min.

### 2.2 Dynamic sitemap
- **File:** `app/sitemap.xml/route.ts` (new) — replace the static `public/sitemap.xml`
- **Action:** Generate sitemap dynamically from the actual data:
  - Hardcoded routes (home, eventos, etc.)
  - All `/historia/[slug]` (4 editions)
  - All `/encuentros/[slug]` (5 encuentro slugs)
  - All `/eventos/[slug]` (8 events)
  - All `/aprender/[slug]` (6 guides)
  - All `/actividades/[slug]` (7 activities)
  - All `/foro/[slug]` (41 threads)
- **Effort:** 1h.

### 2.3 Add the missing-footer-links
- **File:** `components/Footer.tsx`
- **Action:** Add links to `/consentimiento` (photo consent), `/donar` (Phase 3.1), `/manifiesto` (was missing!). Currently the footer has 10+ links but is missing the most important community/trust links.
- **Effort:** 15 min.

### 2.4 Wire the OG image for the home
- **File:** `app/layout.tsx` and any per-page metadata
- **Action:** Currently the OG image is `/og-image.jpg` — a static image. Make per-page OG images (home → maškaráda hero, eventos → next event photo, etc.). For Phase 2: just verify the home's OG image is the right one and it renders. Future: dynamic per page.
- **Effort:** 15 min.

### 2.5 Remove the 8 duplicate `IMG_*.JPG` files
- **Path:** `public/images/event-2026-06-11/`
- **Action:** These 8 raw files are duplicates of the curated set, per memory. Either delete them (saves disk + reduces confusion) or move to a `/_internal/` folder so they're not served at the public URL.
- **Effort:** 5 min.

### 2.6 Add a /donar (donations) page
- **File:** `app/donar/page.tsx` (new)
- **Action:** Per the audit, no way to financially support the platform exists. Kiki decides: bank transfer (SIPAP), Tigo Money, or just a "write to us" page. Default: copy the Monai WhatsApp+transfer pattern.
- **Effort:** 30 min.

### 2.7 Cookie consent banner
- **File:** `components/CookieBanner.tsx` (new) + mounted in `app/layout.tsx`
- **Action:** GDPR requires a cookie consent banner for EU visitors. Simple: a small dismissable banner at the bottom that says "Usamos cookies. Aceptar / Saber más → /privacidad." The `mk_locale` cookie and the session cookie are the only persistent ones.
- **Effort:** 1h.

### 2.8 Remove the placeholders in /galeria
- **File:** `app/galeria/page.tsx`
- **Action:** Currently the gallery is a client-side rendered grid that shows 0 photos on SSR. Either:
  - (a) Make it server-side rendered (read from `public/images/` at build time)
  - (b) Add a clear "Cargando galería..." SSR placeholder with a `<noscript>` fallback
- **Recommend (a)** — for SEO and shareability, gallery should be server-side.
- **Effort:** 1.5h.

---

## Phase 3 — Community features (3-5 days, the big structural stuff)

The features that turn maškaráda from a content site into an actual community platform.

### 3.1 Real authentication + member accounts
- **Files:** `app/(auth)/login/page.tsx` (new), `lib/auth.ts` (new), `app/api/auth/...` (new)
- **Action:** Per the brief, this is gated on a moderation plan + ToS + DMCA process. Until Kiki has those, hold. But design the architecture now so it's plug-in later.
- **Status:** **BLOCKED** on policy work. Don't start until Kiki approves.

### 3.2 Photo upload mechanism (K1 from the original brief)
- **Files:** `app/subir-fotos/[event-slug]/page.tsx` (new), `app/api/photos/route.ts` (new), `app/galeria/[event-slug]/page.tsx` (new)
- **Action:** Per the original K1 discussion: "anyone can paste a Google Drive / online folder URL, system downloads + adds to gallery; people can find themselves." 
- **Approach:** Phase 1 = simple form (paste Drive folder URL + name + email). Admin approves in `/admin/photos`. No face recognition. No OCR. No Drive OAuth. Just "you submit a link, the team downloads, picks the good ones, publishes them."
- **Supabase storage:** store the photos in a Supabase Storage bucket, not in `public/`. Saves disk on the VPS.
- **Status:** Phase 1 of this is a 2-day build. The current state is 9 curated photos on disk, nothing else.
- **Effort:** 2-3 days.

### 3.3 Multi-vendor store with payment
- **Files:** `app/tienda/[vendor-slug]/page.tsx` (exists, needs work), `app/tienda/checkout/[vendor]/page.tsx` (new)
- **Action:** Per I2 research, the recommended rollout is: WhatsApp + bank transfer (current) → Tigo Money (Phase 3.3a) → Bancard (Phase 5+). Phase 3.3 adds a vendor-side dashboard (view orders, mark as paid, update stock). The current `lib/cart.ts` works locally but doesn't sync across devices.
- **Status:** Blocked on real vendor #2 joining. Once that happens, ship this.
- **Effort:** 1 week.

### 3.4 Workshop / class registration
- **Files:** `app/aprender/[slug]/page.tsx` (existing guides), `app/api/workshops/register/route.ts` (new)
- **Action:** Per the brief: workshops with capacity, RSVP, payment. Currently /aprender has guides but no live registration. Add: "Próximo taller: 15 de junio · 8 cupos · Gs 30.000 · Anotarme" with a registration form that goes to `mk_workshop_registrations`.
- **Supabase table:** new.
- **Effort:** 2-3 days.

### 3.5 Munches / workshops listings (K6 already shipped a calendar)
- **Status:** Done. The /eventos page already has the 6-week calendar grid and lists 1 alté/month + 1 maskarada/4-6mo per Kiki's spec. No more work needed here. **Mark complete.**

### 3.6 /aprender — more guides
- **Files:** `lib/guides.ts` (existing), `app/aprender/[slug]/page.tsx` (existing)
- **Action:** Currently 6 guides. Add 6 more to reach the "mature community" bar:
  1. "Hosting a Munch" — practical guide for first-time organizers
  2. "Photo Consent in Kink Communities" — legal + ethical
  3. "Choosing a Kink-Aware Therapist" — referral list
  4. "Negotiating scenes when you don't know what you want" — for beginners
  5. "Aftercare beyond the scene" — 24-72 hours post
  6. "Poly, ENM, and kink — when relationships overlap" — for ENM folks
- **Status:** Pure content work. Needs Kiki's time.
- **Effort:** 4-6h of writing.

---

## Phase 4 — Operational polish (1-2 weeks)

The things that make the platform feel "real" but are not content-blocking.

### 4.1 Search
- **Files:** `components/Search.tsx` (new), `app/api/search/route.ts` (new)
- **Action:** Client-side search using Fuse.js or a similar lightweight lib. Searches page titles, descriptions, and forum thread titles. ~2h to ship. The site has 38 routes and 41 forum threads — without search, discovery is via nav only.
- **Effort:** 2h.

### 4.2 Image manifest cleanup
- **File:** `public/images/event-2026-06-11/manifest.json`
- **Action:** Currently referenced from Kiki's photo workflow. The site doesn't use it. Either delete or wire it into a `/galeria/[event-slug]/manifest.json` that the gallery reads from.
- **Effort:** 30 min.

### 4.3 Per-vendor admin
- **Files:** `app/admin/vendors/page.tsx` (new)
- **Action:** Currently vendors are managed via the existing /admin (tickets/blocklist/marketing tabs). Add a "Vendors" tab that shows pending applications, lets the team approve/reject, edit profiles.
- **Effort:** 1 day.

### 4.4 /aprender/[slug] page styles
- **File:** `app/aprender/[slug]/page.tsx`
- **Action:** The guide pages have text but no hero images. Add a category-themed image at the top of each guide. Reuse the existing event-2026-06-11/ photos with subtle filters.
- **Effort:** 1h.

### 4.5 /actividades/[slug] page styles
- Same as 4.4 but for the 7 activities. Each should have a hero image.
- **Effort:** 1h.

### 4.6 /staff detail pages
- **File:** `app/staff/[member]/page.tsx` (new)
- **Action:** Currently /staff is a flat list. Add per-member pages with bios, photos, what they do at events.
- **Effort:** 4h (content-bound).

### 4.7 Blog/announcements
- **Files:** `app/blog/page.tsx`, `app/blog/[slug]/page.tsx` (new)
- **Action:** "Per-event recaps, photo albums, announcements" — content channel. Kiki's voice.
- **Status:** Out of scope unless Kiki specifically wants it.
- **Effort:** 1 day to scaffold + ongoing.

### 4.8 Email collection
- Currently the contact form and footer newsletter both write to `mk_marketing_list`. Kiki needs to export this list and email people. Build a simple "send broadcast" admin tool.
- **Status:** Pure feature. Not blocking.
- **Effort:** 2 days.

### 4.9 Push notifications for events
- The 120-person Telegram group is the de facto push channel. Build a `/api/notify` endpoint that posts to a Telegram bot when a new event is added.
- **Status:** Out of scope unless Kiki has the bot set up.
- **Effort:** 1 day.

### 4.10 Per-event photo galleries
- **File:** `app/galeria/[event-slug]/page.tsx` (new)
- **Action:** Each event has a photo wall. Currently /galeria is mixed-category. Split by event: /galeria/2026-06-11-simondice, /galeria/2025-12-12-mascaranegra, etc.
- **Effort:** 2-3h.

---

## Phase 5 — Infrastructure (1-2 weeks, mostly Ivan-side)

The things that require ops work, not just code.

### 5.1 Move to Vercel (deferred to next session)
- **Status:** Per the original conversation, this was deferred. Document the migration plan but don't start until next session.
- **Blocker:** Kiki + Ivan decision.

### 5.2 Add service_role key for proper admin auth
- **Files:** `.env` (new), `lib/supabase-admin.ts` (exists)
- **Action:** Currently the admin uses the anon key (RLS-restrictive). To harden: add `SUPABASE_SERVICE_ROLE_KEY` to env, route admin writes through a service_role client, restrict RLS to anon-only reads.
- **Risk:** Once added, anyone with the env var can do anything. Handle with care.
- **Effort:** 4h + careful testing.

### 5.3 Backup strategy
- **Action:** The VPS has no documented backup plan. Set up nightly backups of:
  - `/root/paragu-ai-platform/` (git, but working tree)
  - `/root/maskarada-assets/` (the 169 photos)
  - Supabase (managed backups, but verify retention)
- **Effort:** 4h.

### 5.4 Tailscale peer cleanup
- **Status:** Memory: "2 stuck Tailscale peers rx 0". Logout those peers.
- **Effort:** 5 min.

### 5.5 Disk prune
- **Status:** Memory: "DISK 87% (53GB free)". Run `docker image prune -a --filter until=720h`. Reclaim ~20GB.
- **Effort:** 10 min.

### 5.6 Monitoring
- **Status:** No monitoring on the production VPS. Set up:
  - UptimeRobot or similar for HTTP checks
  - Supabase dashboard alerts for table growth
  - VPS disk/CPU alerts
- **Effort:** 1 day.

### 5.7 SSL renewal verification
- **Status:** Cloudflare-managed. Verify renewal is set up and that the LE cert on Traefik auto-renews.
- **Effort:** 30 min.

---

## Phase 6 — Future / out of scope (parking lot)

These are mentioned in the brief or in the original conversation but not on the near-term roadmap.

| # | Item | Why parked |
|---|---|---|
| P6.1 | Real Discord/Telegram bot integration | Need Kiki to set up the bot first |
| P6.2 | Video uploads (replacing the 3 placeholder videos) | Storage + bandwidth, not needed yet |
| P6.3 | Stripe / Bancard card processing | Phase 5+ per I2 research |
| P6.4 | Face recognition (K1, option B) | Privacy too risky, no current vendor |
| P6.5 | Member-only content | Phase 3.1 prerequisite |
| P6.6 | Vendor reviews | Phase 3.1 prerequisite |
| P6.7 | DMCA / moderation / trust & safety | Phase 3.1 prerequisite |
| P6.8 | Multi-language beyond ES + EN (PT, NL) | Per Kiki: "only do eng and esp" |
| P6.9 | Native mobile apps | Out of scope |
| P6.10 | Real-time chat (DM) | Out of scope |

---

## Sequencing

The phases are designed so each one ships independently and doesn't block the next.

| Phase | What | Effort | When |
|---|---|---|---|
| **1** | 6 bug fixes | 2h | **This week** |
| **2** | 404 page, sitemap, footer links, cookie banner, /donar, gallery SSR | 3-4h | This week |
| **3** | Photo upload (K1), multi-vendor, workshops, more guides | 1-2 weeks | Next 2 weeks |
| **4** | Search, blog, per-event galleries, vendor admin | 1-2 weeks | When Kiki has bandwidth |
| **5** | Vercel migration, service_role, backups, monitoring | 1-2 weeks | When ops has bandwidth |
| **6** | Parking lot | — | Indefinitely |

**Total Phase 1 + Phase 2 effort: ~6-7 hours of coding. Phase 3+: weeks of work that depends on Kiki's inputs (vendor #2, real testimonials, more photos, guide writing).**

---

## How to read this

If you're Kiki and have 1 hour: **Phase 1**, items 1.1 to 1.7. Six bug fixes.

If you're Kiki and have a day: **Phase 1 + Phase 2**. Site becomes production-quality.

If you're Kiki and have a week: **Phase 1 + 2 + 3.1 (K1 photo upload)**. The "community" starts to actually exist.

If you're Ivan: read the **Phase 5** items. They are mostly your call.

The full audit is in the previous message (39 numbered items). This plan maps those into actionable phases.

---

## Open questions for Kiki

1. **Real email for /contacto** — what address? `hola@maskarada.com`, `info@maskarada.com`, or something else?
2. **B5 seed strategy** — should I seed 2-3 missing-things with `claimed_by` to make the page look active, or is the empty state fine?
3. **/donar** — bank transfer, Tigo Money, or just a "write to us" page?
4. **Photo upload (Phase 3.2)** — start now, or wait until there's more than 9 photos? Current state is mostly text + 9 photos.
5. **Per-event galleries (Phase 4.10)** — wait until K1 uploads give us more photos, or scaffold now?
6. **Phase 5.1 Vercel** — when to revisit. The session deferred this. Pick a date.
