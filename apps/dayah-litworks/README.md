# Dayah LitWorks

Book cover design studio — built on ParaguAI Platform.

## Setup

```bash
npm install
npm run dev
```

## Content

Edit `content/es.json` and `content/en.json` for all site copy.
Edit `app/globals.css` for theme customization.

## Deploy

```bash
npm run build
npm start
```
