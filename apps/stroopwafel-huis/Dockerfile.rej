--- Dockerfile
+++ Dockerfile
@@ -9,6 +9,7 @@ WORKDIR /app
 ENV NEXT_TELEMETRY_DISABLED=1
 COPY . .
+RUN mkdir -p /app/content && cp -r /app/src/content /app/content 2>/dev/null || true
 RUN npm run build
 
 FROM node:20-alpine AS runner
 
