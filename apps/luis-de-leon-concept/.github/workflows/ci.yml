name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  ci:
    uses: Ai-Whisperers/ci-cd/.github/workflows/ci-nextjs.yml@main
    with:
      site-url: 'https://luis-de-leon.paragu-ai.com'
      package-manager: 'npm'
      node-version: '20'
    secrets: inherit
